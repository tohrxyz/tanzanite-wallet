import { View } from "react-native";
import TextWrapper from "../../../components/TextWrapper";

export default function ReceiveScreen () {
  return (
    <View className="bg-blue-500 h-full w-full">
      <TextWrapper>Receive Screen</TextWrapper>
    </View>
  )
}